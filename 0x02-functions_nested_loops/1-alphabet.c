#include "main.h"
/**
 * main - check the code
 *
 * Return: Always 0.
*/

int main(void)
{
	char Harold;

	for (Harold = 'a'; Harold <= 'z'; Harold++)
		_putchar(Harold);
	_putchar('\n');
	return (0);
}
