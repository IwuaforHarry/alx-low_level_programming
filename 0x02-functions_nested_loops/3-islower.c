#include "main.h"
/**
 *_islower - program to print lower case letters
 *
 * Return: return 1 when successful otherwise return 0
*/

int _islower(void)
{
	int Harry;

	if (Harry >= 'a' && Harry <= 'z')
		return (1);
	else
		return (0);
}
