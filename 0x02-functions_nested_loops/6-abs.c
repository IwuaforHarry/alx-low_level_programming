#include "main.h"
/**
 * _abs - program to compute an absolute value
 * @t: Value to be printed
 * Return: return exact value of t
*/

int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
