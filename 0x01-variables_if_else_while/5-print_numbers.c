#include<stdio.h>
/**
 * main -A program to print all single base 10 numbers
 *
 * Return: return 0 once successful
*/

int main(void)
{
	int a;

	for (a = 0; a <= 9; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
